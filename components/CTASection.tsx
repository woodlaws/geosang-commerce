import { TrackedLink } from "@/components/TrackedLink";
export function CTASection({ eyebrow, title, description, href, label, tone = "gradient" }: { eyebrow?: string; title: string; description: string; href: string; label: string; tone?: "gradient" | "gold" }) { const analyticsEvent=href.startsWith("/creators")?"click_creator_apply" as const:"click_brand_inquiry" as const; return <section className={`cta-section ${tone}`}><div>{eyebrow && <span>{eyebrow}</span>}<h2>{title}</h2><p>{description}</p></div><TrackedLink href={href} analyticsEvent={analyticsEvent} className="white-button">{label} <b>→</b></TrackedLink></section>; }
