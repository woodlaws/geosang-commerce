import { PageHero } from "@/components/PageHero";
import { operatorDetails, privacyConfig } from "@/data/legal";
import { createPageMetadata } from "@/data/site";
export const metadata = createPageMetadata("개인정보처리방침", "거상커머스 개인정보처리방침", "/privacy");
export default function PrivacyPage(){ const configuredDetails=operatorDetails.filter(([,value])=>Boolean(value)); return <main><PageHero eyebrow="POLICY" title="개인정보처리방침" description="온라인 신청 접수 전 확인해야 할 개인정보 수집·이용 기준을 안내합니다."/><article className="shell legal section-gap"><h2>1. 수집하는 개인정보</h2><p>{privacyConfig.collectedItems.join(", ")} 항목을 신청 유형에 따라 수집합니다.</p><h2>2. 이용 목적</h2><p>{privacyConfig.purposes.join(", ")} 목적으로만 이용합니다.</p><h2>3. 보유 및 이용 기간</h2><p>{privacyConfig.retentionPeriod||"실제 접수 시작 전 보유 기간을 확정하여 이 페이지에 고지합니다."}</p><h2>4. 처리 위탁</h2><p>{privacyConfig.processor||"외부 접수 시스템이 확정되면 처리 위탁 대상과 업무 내용을 고지합니다."}</p><h2>5. 파기 방법</h2><p>{privacyConfig.destruction}</p><h2>6. 동의 거부 권리</h2><p>{privacyConfig.refusal}</p><h2>7. 개인정보 보호 및 문의</h2>{configuredDetails.length?<dl className="legal-details">{configuredDetails.map(([label,value])=><div key={label}><dt>{label}</dt><dd>{value}</dd></div>)}</dl>:<p>운영 주체와 개인정보 문의 정보는 확정된 항목만 설정을 통해 공개합니다.</p>}</article></main> }
